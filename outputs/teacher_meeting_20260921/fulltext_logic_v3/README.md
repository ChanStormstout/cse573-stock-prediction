# Full-text architecture: explicit parallel inputs

The owner rejected the previous example-led composition because the connections were unclear. This standalone one-slide replacement restores the actual data dependencies: independent title, body, reporting and price lanes join once before logistic regression. The new body branch is teal; existing inputs and processing remain blue. Short downward links identify selection/group contributions to reporting features. The target-price illustration is subordinate at the bottom, not an additional model branch.

Main slide text is concise. FinBERT is defined in the subtitle. A short speaker note states frozen encoding, within/across-group averaging, missing-input fallback and proposal status. No new performance or trained-model claims are introduced. The figure remains a proposed full-text extension.

Only local `Slide6_FullText_Architecture_Final.pptx` was produced for manual insertion; Google Slides was not opened or changed. The PPTX contains editable native shapes, text and arrows. No icon library or matplotlib workflow is used. The current request does not need scientific-figure-making, whose stated scope is data plots rather than method architecture.

Two final-slide renders were inspected. The first exposed wrapping in the group label and missing arrowheads on vertical dependencies; these were repaired. Final native rendering has aligned lanes, a single join point and no observed clipping. Package, layout/font policy, one-slide count and artifact-tool reimport checks pass. Validation receipts and previews stay in the private work directory; the source is committed. Build paths identify the local bundled artifact-tool runtime and must be adapted elsewhere. Keep output versioning intact.
