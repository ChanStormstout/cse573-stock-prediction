# Preregistered future matrix

No row has been executed.

| ID | Input |
|---|---|
| P0 | price + market only |
| R0 | P0 + raw current/past text under fixed token budget |
| R1 | P0 + stale-text similarity + dissemination |
| R2 | P0 + ordinary event/history representation |
| R3 | P0 + ECNI factual innovation + separate dissemination |
| R4 | P0 + ECNI + dense semantic channel |

Also run a preregistered 2×2: without/with market × without/with factual innovation.

Every comparison uses identical stock-days, eligible articles, cutoff, price information, market information, comparable predictor capacity and internal selection budget. Separate representation gain from additional-data gain. SEC comparisons are matched with-SEC versus without-SEC.

Report log loss and `G_bits`, plus predeclared classification metrics only after a separate predictive authorization. Dataset/panel/split choices remain outcome blind.

Predictive authorization requires: source hashes and permitted-use record; timestamp/entity audits passed; point-in-time article ledger; duplicate groups; deterministic panel/company/time splits; locked periods sealed; reader gold set and acceptance thresholds frozen; article/token parity tests; price/market provenance; model grids, seeds and selection budgets preregistered; train-save-reload verifier; and a rule forbidding test-driven per-stock winners.
