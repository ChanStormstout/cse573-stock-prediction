# Side-chat code review：修正与验证记录

2026-09-15。本轮实际修改代码并运行保护检查；新实验另存。历史结果没有被改写成修正后的成绩。

## 逐项处理

| Review 项 | 实际修正 | 验证与证据边界 |
|---|---|---|
| 早期同验证集选 C、报分 | E02/E03 代码明确标为旧开发流程并阻止覆盖；E09 在训练期六月/七月/八月向前验证选 C，重新训练价格、文本、联合、FinBERT 等全部主要基线。 | E09 保存权重能重现全部分数；价格/FinBERT 对照与原 E04 训练期选参版本精确一致。旧 E02/E03 分数仍保留为历史开发记录，不能提升为独立泛化成绩。 |
| 结构化试验重启绕过停止条件 | 加载记录后先检查前 20 条质量，只要不足 80%，20、21 或更多条的恢复均拒绝；检查输入次序、股票、记录键、文本哈希、提示词/配置/代码指纹。检查发生在加载 MLX 模型之前。 | 当前旧 20 条结果实际 dry-run 触发持续停止，未继续推理；新的状态文件保存 quality_stopped。没有 matching manifest 的旧结果不自动信任。 |
| 特征缓存漏检 | 正文缓存纳入文本、上游标题概率与推理记录、模型/固定 revision、最大长度、截断/填充、聚合说明、编码代码、torch/transformers/tokenizers 版本、计算精度与设备；语义缓存还纳入有序记录键与标题内容。元数据保存 payload SHA256。 | 输入、配置、模型 revision、payload 变化和只有半个缓存的情况均拒绝。新语义缓存采用 v2 路径；旧缓存保留，不追认新的来源证明。当前历史缓存匹配的证据没有被推翻。 |
| 时间模型准备覆盖旧输入 | 从 prepare 入口保护整个实验目录；准备完成时保存输入、来源与代码哈希。run 先核验，再训练；拒绝已有结果或部分训练产物。E09/E10 采用独立目录。 | 实际调用旧 prepare/run 均在写入前拒绝。E05 准备步骤也补充保护，避免覆盖已审阅的质量样本。 |
| 固定 40 轮过拟合 | E09 在每个训练前缀内部留最后 20 个交易日选择轮数，随后重置种子、重训完整前缀；外层月份只评分。比较原宽度及更小 GRU。 | 新早停轮数、缩放、权重与预测已核验。Brier 降低但 BA 没有明显提高；修复训练控制不等于一定提升方向预测。 |

## 保存与检查

- `original_code/` 保存本轮改动前的 9 个源码文件。
- `before_sha256.json` 锁定 204 个历史结果、特征缓存和质量产物；修正后及新实验检查中仍完全一致。
- `test_guards.py`：5 个有针对性的单元测试，覆盖持续停止、错误恢复、缓存变更、目录保护与来源变更。
- `check_result.txt`：实际入口 dry-run/拒绝覆盖结果、历史哈希及语法核验。
- `environment-freeze.txt`：本轮本地 Python 环境。新增 NLTK 3.9.2 用于 Snowball 词干化，没有下载 NLTK 语料，也没有调用付费服务。

测试命令（从项目根目录运行）：

```bash
work/stock-data/finbert-env/bin/python -m unittest discover -s outputs/stock_review_fixes -p 'test_guards.py' -v
work/stock-data/finbert-env/bin/python outputs/stock_structured/pilot.py --dry-run
```

第二条预期抛出质量停止错误，这是保护生效，不是需要绕过的故障。

现有训练目录故意不能直接重跑覆盖。另开实验时先固定新的输出/缓存位置和代码快照，再登记协议、准备数据、训练；检查已有模型使用各实验的 `check.py`。本轮没有把历史目录当作可随意重建的临时文件。

## 总体判断

Review 指出的是确实需要修正的评估与工程保护问题；本轮没有发现证据足以说全部旧实验无效。早期选参分数须降格为开发记录；未发生的缓存失配或重启越过停止条件不能倒推为已经发生。所有新结果也仍是开发集探索，因为此前的开发结果已经影响方法设计；最终测试未评价。
