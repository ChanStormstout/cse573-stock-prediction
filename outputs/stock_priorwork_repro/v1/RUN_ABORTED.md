# Aborted before predictive artifacts

The first invocation stopped on a classifier-name normalization error before it
wrote predictions, metrics, models, or scientific results. Its manifest and
private inputs are preserved. The corrected, unchanged preregistration is run
in immutable `v2`; this is a software recovery, not a rerun or a tuned variant.
