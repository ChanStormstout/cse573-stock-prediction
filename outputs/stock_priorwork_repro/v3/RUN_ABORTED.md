# Aborted before predictive artifacts

The third invocation stopped during the bounded classifier grid before writing
predictions or metrics. It is preserved as an incomplete artifact. The v4 run
uses the same frozen protocol and will capture a complete runner log for the
post-run record.
