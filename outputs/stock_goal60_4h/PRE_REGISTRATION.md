# New-information / dense-transfer round — registered before results

Main task remains all 1607 AAPL/AMZN four-hour direction windows, cutoff five minutes before start. No new labels, no outcome filtering, no per-stock hindsight architecture winners. Existing periods are exposed historical backtests. 60% for both stocks is a goal, not a promised result. User authorized this prospectively BA-prioritized protocol; previous Brier-gated failures remain unchanged.

## Evaluation and system selection

March–May are chronological inner forward predictions; June/July/August are outer train-period evaluation, chosen using only strictly earlier months. Fit using all eligible past windows with realized label end < evaluation cutoff. Fit PCA/scalers/vocab only on past data. Final September-onward system freezes at August end; development and later reported separately, never averaged to select. Selected outer averages are development evidence, not an independent test of architecture selection.

Each month select ONE hyperparameter configuration globally across both stocks by minimum stock mean monthly BA, then macro BA, then smaller capacity, using earlier forward months. C=.01/.1/1, March default .1. Use final issued predictions including fallback for selection; retain raw predictions in private diagnostics. All direction thresholds .5. Brier/MCC/constant prediction rates reported, Brier not a veto. Positive temperature-only calibration is a separate past-OOF probability diagnostic, preserving direction. No threshold tuning this round. Early stop for interactions is trained only on last20% of past complete dates; train labels overlapping that internal validation cutoff removed, then refit all past data for chosen epoch count.

## P: cross-asset price

R1 OLD+RECENT columns as existing. X adds 15/60m other-stock returns/realized volatility, same-window relative returns, prior-complete-session other-stock return/range, relative prior-day return, and missing flags. Reconstruct from raw bars at exact cutoff; never require another stock's target label or four-hour sample to exist. Previous session uses only complete bars; before open intraday values missing. Own/other daily state counted explicitly. Six cells: R1 vs R1+X × L2 LR / shallow HistGradientBoosting / synthetic-only TabPFN2.5. Boosting fixed depth3,100 rounds,lr.05,min_leaf20,l2=1,no random validation. TabPFN fixed checkpoint tabpfn-v2.5-classifier-v2.5_default.ckpt at HF revision6c45f3a6d0d07c6c5f62572e04a0c2929de91b8b; one estimator, seed573, local CPU, no auto-search or telemetry. If genuinely unavailable, preserve attempted error and continue other cells. Fixed LR/boosting seeds573. No paid data/API.

## T: matched dense transfer

Freeze existing A1 (trained on249 provisional January–February articles, last availability February28); use seeds573/574/575, never pick best seed. Original FinBERT and A1 read IDENTICAL target/title/evidence-candidate/context tokens, CLS pooling, max512 protected-candidate chunks. For ALL original article-target pairs, retrieve first two complete target-company sentences in body order with adjacent context; no rating keyword filter. If absent, title candidate fallback is marked. Same candidate set and no event gate for dense features. Original FinBERT branch used once; A1 branches use3 checkpoints. PCA16 trained on past article-target vectors. Price input fixed R1 for four core cells: original/A1 × additive L2 LR / rank2 bilinear interaction. Interaction uses first8 specified state features (recent returns15/60,rv15/60,old mean/std,minutes_from_open,overnight_gap), hidden text16; lr.001, weight_decay.1, max80 epochs, patience8, full-batch, seeds573/574/575, average all seed probabilities. A1 seed pairs with same interaction seed; each PCA independently fits past features (no mean of incompatible spaces). Encoder A1 availability March1; January–February article representations may be re-encoded for later supervised fits, but no earlier stock OOF prediction uses A1. No new encoder gradient training.

Recompute F1/F2 with actual-system C selection as historical matching controls. Add one cached title-semantic8+Modern8 LR control (same price and no-news fallback), PCA per encoder per past training articles. No changes to previous input filtering or labels.

## H: historical news state

Before training audit coverage from currently accepted target-news universe. Preserve current title filters/time validity; use current and prior2 scheduled trading sessions, bounded to earliest prior2-session open <= available <= cutoff, include intervening overnight news. H0=original4h set, H1=H0 plus older accepted articles outside H0 in that3-session range, separate buckets. Use frozen original FinBERT title means, train-article PCA8 shared across both buckets, H0 old bucket zeros to match dimension. Metadata counts/age/missing; H2 same H1 articles plus available-to-cutoff observed returns/volatility (no semantic event-change claims). Reactions start at first scheduled bar start >= availability; only completed bars end<=cutoff; missing intervals cause unknown; overnight-spanning reactions marked and not treated as contiguous intraday. H0/H1/H2 same R1+LR, original no-news windows may gain history; fallback only if no allowed article in either bucket. Original news flag remains separate from historical-state flag. If extra qualifying title embeddings needed, same pinned ProsusAI model/pooling, encode without labels. AMZN coverage counts are measured, not assumed.

## External data feasibility

Fixed Alpaca authenticated sample task SPY/QQQ January10 2018, 1Min, inspect feed, adjustment, timestamp and completeness. No available credentials => AUTH_REQUIRED, no fabricated data or claims about coverage; do not delay other experiments. Only if adequate data obtained define separate preregistered market-feature branch/new-period holdout before viewing labels; no silent dataset mixing.

## Advancement and combinations

For each matched contrast on June–August: weaker-stock mean BA improves>=1pp, neither stock drops>1pp, and macro difference positive in>=2 of3 months. A stock may remain below60: this is an exploration gate, not success criterion. Inspect predefined common-error/no-original-news strata; don't use these outcomes to create ad hoc rules. Top2 passing mechanisms by same score may enter at most2 finite probability combinations with F1_new, weights0/.25/.5/.75/1 globally selected only on earlier issued OOF. Missing mechanisms not invented. Full system June–August selectors use only earlier candidate outcomes; final choice uses train only. If none passes, no combinations. This round does not fit new GNN, large encoder, RL or new labels.

## Outputs / safeguards

Independent directory outputs/stock_goal60_4h/v1 and ignored work/stock-data/goal60_4h/v1. Hash content/config/checkpoints; reject inconsistent resume/overwrite. Save all fitted model weights privately, 1607-key predictions (warmup empty), monthly/stock/strata/seed scores, train-only selection evidence, timestamps/reload tests,1/5-day joint-date paired intervals and fixed-hash cases. Public files have no raw news/price features, environments or weights. Update course report/status/log/demo, refresh/check repository, commit/push/verify remote.
