# 2026-09-20 update: owner-directed pause and FULL semantic extension

Original LLM inference and continuation are suspended after 1,972 saved responses; see outputs/stock_random_protocol_4h/v1/PAUSE_AUDIT.json. Preserve process memory and output hashes; do not restart the automatic chain. Side chat has exposed ordinary and grouped random outer scores. Earlier sealed-score statements below describe historical state only and are superseded for current use. All subsequent same-split work is exploratory random backtesting. New isolated finite traditional comparisons and target-paragraph semantic extensions are authorized; old frozen protocol remains unchanged.

# Random-protocol four-hour study handoff

## Scope and authority

Owner authorized implementation of the staged random comparison and full
news-context idea on 2026-09-20. The subsequent message permits overlap diagnostics;
this does not require contaminating the new holdout comparison. No old result,
model, gate, external entity review or relation-reader lane was changed.
Starting HEAD cb0acaf4b4186baee7d73ae8a18afa338481472b. Preregistration checkpoint
d74c686fec95b1132be962214bbfbb539de78a65 was pushed before baseline fitting.

## Completed

- Source-bound 1,607-window preparation and 60 outer / 180 inner partitions.
- Corrected Modern v2 pooling vectors (frozen) matched with FinBERT inputs.
- Five baselines: 3,000 fits, 300 selected saved models, sealed outer predictions.
- Independent no-fit replay of 24,105 random probabilities: max error 3.33e-16.
- Grouped/chronological baselines completed: 3,910 fits, 370 selected models,
  30,975 replayed probabilities, max error 4.44e-16; verifier PASS.
- 13 executable contract tests including corruption and exact fallback.
- 240 analogy contexts, 48,210 queries, 45,045 allowed matured case references.
- 7,851 unique fixed prompts, max 3,868 tokens, no truncation.
- 331 candidate fact pairs and 54 frozen outcome-blind pilot cards.

## Active local work — inspect before launching anything

Private root: work/stock-data/random_protocol_4h/v1.
- inference.log / analogy/generated.jsonl: running local Qwen3.5-9B 4bit.
- continuation.log: existing process waits, then runs verify_retrieval.py,
  verify_inference.py, analogy_evaluate.py, fact_pilot_infer.py. It NEVER starts
  a second analogy inference. An eight-hour timeout or traceback stops it.
- diagnostic_training.log: grouped random and chronological baseline training
  is complete; verify_diagnostics.py PASS. Scores remain sealed.
- outputs/stock_random_protocol_4h/v1/*PROGRESS*.json and CONTINUATION_STATUS.json
  are live local files, not a guarantee a process remains alive. Inspect process
  status and logs. Hash-checked resume exists; never overwrite incomplete blocks.

## Still required before a final scientific report

1. Confirm inference completion and independent output verification.
2. Verify finite shared analogy corrections and exact no-case fallback.
3. Review the 54-pair reader pilot against original passages. Mechanical validity
   is not semantic correctness. No independent human labels currently exist.
4. Implement/validate the <=12-feature fact-change residual and genuinely nested
   meta-training if the quality/coverage protocol permits. This component is NOT
   implemented or trained merely because analogy code exists.
5. Complete grouped/chronological mechanism comparisons; only their five baseline
   structures have been scheduled so far.
6. Add preregistered optional classical/Chronos extensions if proceeding; do not
   claim the entire earlier 20-method list has run.
7. Freeze or document feasibility stops, then release main/ablation metrics,
   paired intervals, cases and update slides with measured numbers only.

## Important implementation boundaries

Saved selected inner OOF values are marked NOT_META_TRAIN: finite hyperparameter
selection may use them, learned residual fitting requires deeper cross-fitting.
LLM historic cases must be both past/matured and allowed by the fold. The analogy
correction is a shared finite alpha rule, not the proposed learned fact-change
correction. No result can select an exposed per-stock winner.

The initial MLX launch failed pre-call because scipy was imported indirectly;
this was repaired by isolating standard-library IO. Traceback preserved. A
pre-execution syntax error in the fact-pilot reader was caught by the new tests,
repaired before any pilot call, and the failed test record retained. Neither
repair changed a trained model or prompt. sklearn emits a deprecated penalty
warning; liblinear still receives the registered L1/L2 penalty.

Preserve pre-existing user changes in .gitignore and docs/EXPERIMENT_INDEX.md.
When refreshing manifests, save and restore their exact bytes; force-add only
reviewed new allowlisted JSONs. Do not publish raw passages or checkpoints.
